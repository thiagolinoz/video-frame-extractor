apiVersion: v1
kind: Service
metadata:
  name: video-frame-extractor-service
spec:
  selector:
    app: video-frame-extractor
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
